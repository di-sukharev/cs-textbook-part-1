// Программа: Mult.asm 
// Умножает R0 на R1 и сохраняет результат в R2.
// (R0, R1, R2 — это RAM[0], RAM[1] и RAM[2], соответственно)

// Перед написанием программы прочитайте инструкцию README.md,
// она находится в папке с этим проектом.

